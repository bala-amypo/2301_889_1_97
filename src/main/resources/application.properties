
# Project Identity
spring.application.name=digital-certificate-generator

# Server Configuration
server.port=9001
server.forward-headers-strategy=framework

# Database Configuration
# Ensure the database name matches your local setup
spring.datasource.url=jdbc:mysql://localhost:3306/CertificateGenerator?createDatabaseIfNotExist=true
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate / JPA Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
# Ensures standard naming (e.g., student_id instead of studentid)
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# ======================================================
# JWT CONFIGURATION (Matches Section 8.1)
# ======================================================
jwt.secret=9a67471a2bc916670c538749a04473874052345d6255656241762354124a63
jwt.expiration=3600000

# ======================================================
# SWAGGER CONFIGURATION (Strict Requirement Section 8.4)
# ======================================================
# This ensures Swagger UI is exactly at /swagger-ui/index.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.operationsSorter=alpha